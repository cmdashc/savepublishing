{
  "name": "quotelink",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Quotelink (working title): click a sentence in any article, share it with attribution. MV3 revival of the SavePublishing bookmarklet.",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/jsdom": "^28.0.3",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
